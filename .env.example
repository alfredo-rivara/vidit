# Vidit Configuration
# Copy this to .env and adjust values as needed

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=vidit
DB_SSLMODE=disable

# Server Configuration
PORT=3000

# Note: Make sure PostgreSQL is running before starting the server
# Create database: createdb vidit
